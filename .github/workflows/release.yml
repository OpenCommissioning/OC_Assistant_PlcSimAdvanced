name: Release

on:
  push:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Install Versionize
      run: dotnet tool install --global Versionize

    - name: Setup git
      run: |
        git config --global user.email opencommissioning@spiratec.com
        git config --global user.name "oc-bot"
      
    - name: Versioning
      id: versionize
      run: versionize
      continue-on-error: true

    - name: Get current version
      if: steps.versionize.outcome == 'success'
      run: echo "VERSION=v$(versionize inspect)" >> $GITHUB_ENV

    - name: Get current changelog
      if: steps.versionize.outcome == 'success'
      run: echo "$(versionize changelog)" > latest_changelog.md
    
    - name: Push changes to GitHub
      if: steps.versionize.outcome == 'success'
      uses: ad-m/github-push-action@master
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        branch: ${{ github.ref }}
        tags: true

    - name: Download base64 encoded plcsim dll and decode it
      if: steps.versionize.outcome == 'success'
      env:
        GH_TOKEN: ${{ secrets.GH_GIST_PAT }}
        GIST_ID: ${{ secrets.GIST_ID }}
      run: |
        RAW_URL=$(gh api /gists/$GIST_ID | jq -r '.files.plcsimDLL.raw_url')
        curl -L "$RAW_URL" > plcsim-base64.txt
        base64 -d plcsim-base64.txt > OC.Assistant.Sdk/Siemens.Simatic.Simulation.Runtime.Api.x64.dll   

    - name: Dotnet build
      if: steps.versionize.outcome == 'success'
      run: dotnet build OC.PlcSimAdvanced --configuration release --output ./Publish

    - name: Publish new release
      if: steps.versionize.outcome == 'success'
      run: |
        gh release create ${{env.VERSION}} -t "Release ${{env.VERSION}}" -F latest_changelog.md
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: Upload release artifact
      if: steps.versionize.outcome == 'success'
      run: |
        gh release upload ${{env.VERSION}} ./Publish/OC.PlcSimAdvanced.dll
        gh release upload ${{env.VERSION}} ./Publish/OC.PlcSimAdvanced.plugin
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
